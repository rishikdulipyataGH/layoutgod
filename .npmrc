# NPM configuration for workspace optimization
# Reduces duplicate peer dependencies
legacy-peer-deps=true

# Use faster package resolution
prefer-online=true

# Save exact versions (more predictable builds)
save-exact=false

# Optimize for CI/production builds
fund=false
audit=false

# Engine strict mode (ensure Node version compatibility)
engine-strict=true
